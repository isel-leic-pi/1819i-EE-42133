<div>
    <h4>Leagues:</h4>
</div>
<div class="container mt-3">
    <div class="row">
        <div class="card-deck">
            {{#each this}}
            <div class="card mb-4" style="min-width: 18rem; max-width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">{{name}}</h5>
                    <!--
                    <p class="card-text">{{id}}</p>
                    <p class="card-text">{{area.name}}</p>
                    -->
                    <button type="button" class="btn btn-primary" id="teamsFromLeagueButton" role="button">
                        Teams
                    </button>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</div>